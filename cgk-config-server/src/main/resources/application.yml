server:
  port: 7070
spring:
  application:
    name: cgk-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Toshibby/cgk-intento.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main
  security:
    user:
      name: root
      password: 123456